# 메일 발송 오류 해결 가이드

## 현재 상황

메일 발송에 계속 오류가 발생하고 있습니다.

## 즉시 확인해야 할 사항

### 1. Edge Function 로그 확인 (가장 중요!)

Supabase 대시보드에서 실제 오류 메시지를 확인하세요:

1. **Supabase 대시보드 접속**
   - https://supabase.com/dashboard
   - 프로젝트 선택

2. **Edge Functions → send-evaluation-email → Logs**
   - 최근 실행 로그 확인
   - **error** 레벨 로그 확인
   - 실제 오류 메시지 복사

3. **확인할 로그 메시지**
   - "=== SMTP 인증 시작 ==="
   - "사용자명 인증 시도"
   - "사용자명 인증 응답"
   - "비밀번호 인증 시도"
   - "비밀번호 인증 응답"
   - "❌ 비밀번호 인증 실패" 또는 다른 오류 메시지

### 2. 네이버 메일 외부 프로그램 사용 설정 (필수!)

**이 설정이 꺼져 있으면 SMTP 인증이 절대 성공하지 않습니다!**

#### 확인 방법:
1. https://mail.naver.com 접속
2. `beeper9` 계정으로 로그인
3. 환경설정 → POP3/IMAP 설정
4. **"외부 메일 프로그램에서 메일 읽기"** 확인
5. **"사용함"**으로 설정되어 있는지 확인
6. 설정이 "사용 안 함"이면 **"사용함"**으로 변경 후 저장

#### 직접 URL 접속:
https://mail.naver.com/#setting/pop3

### 3. 네이버 계정 비밀번호 확인

현재 코드에서 사용하는 비밀번호: `kimjungbae99`

#### 확인 방법:
1. https://mail.naver.com 접속
2. 아이디: `beeper9`
3. 비밀번호: `kimjungbae99`
4. 로그인 성공 여부 확인

#### 로그인 실패 시:
- 실제 비밀번호 확인
- 코드의 `naverPassword` 값을 실제 비밀번호로 변경
- 또는 네이버 계정 비밀번호를 `kimjungbae99`로 변경

### 4. 2단계 인증 확인

네이버 계정에 2단계 인증이 활성화되어 있다면:
- 일반 비밀번호 대신 **앱 비밀번호** 사용 필요
- 네이버 계정 → 내 정보 → 보안 설정 → 2단계 인증 → 앱 비밀번호 생성
- 생성된 앱 비밀번호를 코드의 `naverPassword`에 설정

## 현재 코드 설정

```typescript
// 계정명
const naverAccount = 'beeper9'

// 전체 이메일 주소 (자동 구성)
const naverEmail = 'beeper9@naver.com'

// 비밀번호
const naverPassword = 'kimjungbae99'

// SMTP 서버
const smtpServer = 'smtp.naver.com'
const smtpPort = 465

// 사용자명 (SMTP 인증 시)
const username = 'beeper9'  // 아이디만 사용
```

## 일반적인 오류 메시지와 해결 방법

### 오류 1: "535 5.7.1 Username and Password not accepted"

**원인:**
- 네이버 메일 외부 프로그램 사용 설정이 꺼져 있음
- 비밀번호가 잘못됨
- 2단계 인증 활성화되어 앱 비밀번호 필요

**해결:**
1. 네이버 메일 외부 프로그램 사용 설정 확인
2. 비밀번호 확인
3. 2단계 인증 확인 및 앱 비밀번호 생성

### 오류 2: "SMTP 서버 연결 실패"

**원인:**
- 네트워크 문제
- Supabase Edge Function의 네트워크 제한
- 방화벽 차단

**해결:**
- Edge Function 로그에서 상세 오류 확인
- 네트워크 연결 테스트

### 오류 3: "EHLO 실패"

**원인:**
- SMTP 서버 응답 문제
- 네트워크 연결 불안정

**해결:**
- Edge Function 로그에서 실제 응답 확인
- 네트워크 연결 확인

## 문제 해결 체크리스트

다음 항목을 순서대로 확인하세요:

- [ ] **1단계**: Supabase 대시보드에서 Edge Function 로그 확인
  - 실제 오류 메시지 확인
  - "=== SMTP 인증 시작 ===" 메시지 확인
  - "사용자명 인증 응답" 확인
  - "비밀번호 인증 응답" 확인

- [ ] **2단계**: 네이버 메일 외부 프로그램 사용 설정 확인
  - https://mail.naver.com/#setting/pop3 접속
  - "외부 메일 프로그램에서 메일 읽기" → "사용함" 확인
  - 설정이 "사용 안 함"이면 "사용함"으로 변경

- [ ] **3단계**: 네이버 계정 비밀번호 확인
  - 네이버 메일 로그인 테스트 (아이디: beeper9, 비밀번호: kimjungbae99)
  - 로그인 성공 여부 확인

- [ ] **4단계**: 2단계 인증 확인
  - 네이버 계정 → 내 정보 → 보안 설정 확인
  - 2단계 인증 활성화 여부 확인
  - 활성화되어 있다면 앱 비밀번호 생성

- [ ] **5단계**: 코드 확인 및 재배포
  - 코드의 `naverPassword` 값 확인
  - 필요시 수정 후 Edge Function 재배포

- [ ] **6단계**: 재테스트
  - 평가 입력 후 이메일 발송 테스트
  - Edge Function 로그 확인

## 다음 단계

1. **먼저**: Supabase 대시보드에서 Edge Function 로그 확인
   - 실제 오류 메시지를 복사하여 공유해주세요

2. **두 번째**: 네이버 메일 외부 프로그램 사용 설정 확인
   - 가장 중요한 설정입니다

3. **세 번째**: 네이버 계정 비밀번호 확인
   - 로그인 테스트로 확인

4. **네 번째**: 2단계 인증 확인
   - 활성화되어 있다면 앱 비밀번호 생성

## 도움이 필요한 경우

위의 모든 사항을 확인했는데도 문제가 지속되면:

1. **Edge Function 로그의 실제 오류 메시지**를 공유해주세요
2. **네이버 메일 설정 스크린샷**을 공유해주세요
3. **네이버 계정 로그인 테스트 결과**를 알려주세요

이 정보를 바탕으로 더 정확한 해결 방법을 제시할 수 있습니다.

