# 네이버 SMTP 설정 가이드

## 비밀번호 인증 실패 해결 방법

"535 5.7.1 Username and Password not accepted" 오류가 발생하는 경우, 다음을 확인하세요.

## ⚠️ 중요: 네이버 메일 외부 프로그램 사용 설정 (필수!)

네이버 메일에서 SMTP를 사용하려면 **반드시** 외부 메일 프로그램 사용 설정이 활성화되어 있어야 합니다.
이 설정이 꺼져 있으면 "535 5.7.1 Username and Password not accepted" 오류가 발생합니다.

### 설정 방법 (단계별):
1. **네이버 메일 접속**: https://mail.naver.com
2. **로그인**: beeper9@naver.com 계정으로 로그인
3. **환경설정 메뉴**:
   - 우측 상단 **⚙️ 설정 아이콘** 클릭
   - 또는 우측 상단 **프로필** → **환경설정**
4. **POP3/IMAP 설정**:
   - 왼쪽 메뉴에서 **"POP3/IMAP 설정"** 클릭
   - 또는 **"메일 설정"** → **"POP3/IMAP 설정"**
5. **외부 메일 프로그램 사용 활성화**:
   - **"외부 메일 프로그램에서 메일 읽기"** 옵션 찾기
   - **"사용함"** 또는 **"활성화"**로 변경
   - **저장** 또는 **적용** 버튼 클릭

### 확인 방법:
- 설정 후 페이지를 새로고침하여 "사용함" 상태인지 확인
- 다른 브라우저나 시크릿 모드에서도 확인 가능

## 2. 네이버 계정 비밀번호 확인 (중요!)

현재 코드에서 사용하는 비밀번호: `kimjungbae99`

### 확인 방법:
1. **네이버 메일 로그인 테스트**:
   - https://mail.naver.com 접속
   - 아이디: `beeper9`
   - 비밀번호: `kimjungbae99`
   - 로그인이 성공하는지 확인

2. **비밀번호가 다른 경우**:
   - 실제 비밀번호를 확인
   - 코드의 `naverPassword` 값을 실제 비밀번호로 변경
   - 또는 네이버 계정 비밀번호를 `kimjungbae99`로 변경

### ⚠️ 주의:
- 비밀번호가 정확하지 않으면 SMTP 인증이 실패합니다
- Edge Function 로그에서 "비밀번호 인증 시도" 메시지를 확인하여 실제 전송되는 비밀번호를 확인할 수 있습니다

## 3. 2단계 인증 및 앱 비밀번호 (필요한 경우)

네이버 계정에 **2단계 인증**이 활성화되어 있다면, 일반 비밀번호 대신 **앱 비밀번호**를 사용해야 합니다.

### 2단계 인증 확인:
1. 네이버 메일에 로그인
2. 우측 상단 프로필 → **내 정보**
3. **보안 설정** 확인
4. 2단계 인증이 활성화되어 있는지 확인

### 앱 비밀번호 생성 방법 (2단계 인증 활성화 시):
1. 네이버 메일에 로그인
2. 우측 상단 프로필 → **내 정보**
3. **보안 설정** → **2단계 인증**
4. **앱 비밀번호** 메뉴 클릭
5. **앱 비밀번호 생성** 클릭
6. 앱 이름 입력 (예: "SMTP 이메일 발송")
7. 생성된 앱 비밀번호 복사 (한 번만 표시됨!)
8. 코드의 `naverPassword`에 설정

### 코드 수정:
```typescript
// supabase/functions/send-evaluation-email/index.ts (74번째 줄)
const naverPassword = '생성된_앱_비밀번호' // 앱 비밀번호로 변경
```

### ⚠️ 중요:
- 2단계 인증이 활성화되어 있으면 일반 비밀번호로는 SMTP 인증이 실패합니다
- 반드시 앱 비밀번호를 생성하여 사용해야 합니다

## 4. 사용자명 형식

현재 코드는 **아이디만** (`beeper9`)을 사용자명으로 사용합니다.

### 현재 설정:
```typescript
// supabase/functions/send-evaluation-email/index.ts (334번째 줄)
const username = senderEmail.split('@')[0] // beeper9@naver.com -> beeper9
```

### 전체 이메일 주소를 사용하는 경우:
일부 네이버 계정은 전체 이메일 주소를 사용해야 할 수도 있습니다.
코드에서 다음 부분을 수정:
```typescript
// 아이디만 (현재)
const username = senderEmail.split('@')[0] // beeper9

// 전체 이메일 주소로 변경
const username = senderEmail // beeper9@naver.com
```

### 테스트 방법:
1. 먼저 아이디만 사용 (현재 설정)
2. 인증 실패 시 전체 이메일 주소로 변경
3. Edge Function 로그에서 "사용자명 인증 시도" 메시지 확인

## 5. 네이버 보안 정책

네이버는 보안상의 이유로 다음을 제한할 수 있습니다:
- 특정 IP에서의 SMTP 접근
- 일일 발송 제한
- 외부 앱 접근 제한

문제가 지속되면:
- 네이버 고객센터에 문의
- 네이버 보안 설정 확인

## 6. 디버깅

Edge Function 로그에서 다음을 확인하세요:
- "사용자명 인증 시도" 메시지
- "비밀번호 인증 시도" 메시지
- 실제 전송되는 사용자명과 비밀번호(마스킹됨)

## 현재 설정

- **이메일**: beeper9@naver.com
- **비밀번호**: kimjungbae99
- **사용자명 형식**: 전체 이메일 주소 (beeper9@naver.com)
- **SMTP 서버**: smtp.naver.com:465 (SSL/TLS 직접 연결)

## 체크리스트 (순서대로 확인하세요!)

1. **네이버 메일 외부 프로그램 사용 설정** (가장 중요!)
   - [ ] 네이버 메일 접속: https://mail.naver.com
   - [ ] 환경설정 → POP3/IMAP 설정
   - [ ] "외부 메일 프로그램에서 메일 읽기" → "사용함"으로 설정
   - [ ] 저장 후 페이지 새로고침하여 확인

2. **네이버 계정 비밀번호 확인**
   - [ ] 네이버 메일 로그인 테스트 (아이디: beeper9, 비밀번호: kimjungbae99)
   - [ ] 로그인 성공 여부 확인
   - [ ] 실패 시 실제 비밀번호 확인 또는 비밀번호 변경

3. **2단계 인증 확인**
   - [ ] 네이버 계정 → 내 정보 → 보안 설정 확인
   - [ ] 2단계 인증 활성화 여부 확인
   - [ ] 활성화되어 있다면 앱 비밀번호 생성
   - [ ] 코드의 `naverPassword`를 앱 비밀번호로 변경

4. **Edge Function 재배포**
   - [ ] Supabase 대시보드에서 Edge Function 재배포
   - [ ] 또는 CLI: `supabase functions deploy send-evaluation-email`

5. **테스트 및 로그 확인**
   - [ ] 평가 입력하여 이메일 발송 테스트
   - [ ] Edge Function 로그 확인:
     - "네이버 SMTP 설정" 메시지
     - "사용자명 인증 시도" 메시지
     - "비밀번호 인증 시도" 메시지
     - "비밀번호 인증 응답" 메시지

## 문제 해결 순서

1. **먼저 확인**: 네이버 메일 외부 프로그램 사용 설정이 활성화되어 있는지 확인
2. **두 번째**: 네이버 계정 비밀번호가 `kimjungbae99`인지 확인
3. **세 번째**: 2단계 인증이 활성화되어 있다면 앱 비밀번호 생성 및 사용
4. **네 번째**: Edge Function 재배포 후 테스트
5. **마지막**: Edge Function 로그에서 상세 오류 메시지 확인

