# ⚠️ 네이버 SMTP 인증 실패 - 필수 확인 사항

## 오류 메시지
```
535 5.7.1 Username and Password not accepted
```

이 오류는 **네이버 SMTP 서버가 사용자명과 비밀번호를 거부**하고 있다는 의미입니다.

## 🔴 가장 중요한 확인 사항 (순서대로!)

### 1. 네이버 메일 외부 프로그램 사용 설정 (필수!)

**이 설정이 꺼져 있으면 SMTP 인증이 절대 성공하지 않습니다!**

#### 확인 및 설정 방법:

1. **네이버 메일 접속**
   - https://mail.naver.com
   - `beeper9` 계정으로 로그인

2. **환경설정 메뉴 찾기**
   - 방법 1: 우측 상단 **⚙️ 설정 아이콘** 클릭
   - 방법 2: 우측 상단 **프로필 사진** → **환경설정**
   - 방법 3: URL 직접 접속: https://mail.naver.com/#setting/pop3

3. **POP3/IMAP 설정 메뉴**
   - 왼쪽 메뉴에서 **"POP3/IMAP 설정"** 클릭
   - 또는 **"메일 설정"** → **"POP3/IMAP 설정"**

4. **외부 메일 프로그램 사용 활성화**
   - **"외부 메일 프로그램에서 메일 읽기"** 옵션 찾기
   - 현재 상태가 **"사용 안 함"** 또는 **"비활성화"**인지 확인
   - **"사용함"** 또는 **"활성화"**로 변경
   - **저장** 또는 **적용** 버튼 클릭

5. **확인**
   - 페이지 새로고침 (F5)
   - 설정이 **"사용함"**으로 변경되었는지 확인
   - 다른 브라우저나 시크릿 모드에서도 확인

#### ⚠️ 중요:
- 이 설정이 **"사용 안 함"**이면 SMTP 인증이 **절대** 성공하지 않습니다
- 설정 변경 후 **몇 분 정도 기다린 후** 다시 시도하세요

---

### 2. 네이버 계정 비밀번호 확인

현재 코드에서 사용하는 비밀번호: `kimjungbae99`

#### 확인 방법:

1. **네이버 메일 로그인 테스트**
   - https://mail.naver.com 접속
   - 아이디: `beeper9`
   - 비밀번호: `kimjungbae99`
   - 로그인 버튼 클릭

2. **결과 확인**
   - ✅ **로그인 성공**: 비밀번호가 정확합니다. 다른 원인을 확인하세요.
   - ❌ **로그인 실패**: 비밀번호가 다릅니다!

#### 비밀번호가 다른 경우:

**옵션 A: 코드 수정 (권장)**
```typescript
// supabase/functions/send-evaluation-email/index.ts (74번째 줄)
const naverPassword = '실제_비밀번호' // 실제 비밀번호로 변경
```

**옵션 B: 네이버 계정 비밀번호 변경**
- 네이버 계정 설정에서 비밀번호를 `kimjungbae99`로 변경

---

### 3. 2단계 인증 및 앱 비밀번호 확인

네이버 계정에 **2단계 인증**이 활성화되어 있다면, 일반 비밀번호 대신 **앱 비밀번호**를 사용해야 합니다.

#### 확인 방법:

1. **네이버 메일 접속**
   - https://mail.naver.com
   - 로그인

2. **보안 설정 확인**
   - 우측 상단 **프로필** → **내 정보**
   - **보안 설정** 메뉴 클릭
   - **2단계 인증** 상태 확인

3. **2단계 인증이 활성화되어 있는 경우**

   **앱 비밀번호 생성:**
   1. **보안 설정** → **2단계 인증** → **앱 비밀번호**
   2. **앱 비밀번호 생성** 클릭
   3. 앱 이름 입력 (예: "SMTP 이메일 발송")
   4. 생성된 앱 비밀번호 복사 (⚠️ 한 번만 표시됨!)
   5. 코드의 `naverPassword`에 설정

   **코드 수정:**
   ```typescript
   // supabase/functions/send-evaluation-email/index.ts (74번째 줄)
   const naverPassword = '생성된_앱_비밀번호' // 앱 비밀번호로 변경
   ```

#### ⚠️ 중요:
- 2단계 인증이 활성화되어 있으면 일반 비밀번호로는 SMTP 인증이 실패합니다
- 반드시 앱 비밀번호를 생성하여 사용해야 합니다

---

## 현재 코드 설정

```typescript
// supabase/functions/send-evaluation-email/index.ts
const naverEmail = 'beeper9@naver.com'
const naverPassword = 'kimjungbae99'
const smtpPort = 465  // SSL/TLS 직접 연결
const username = 'beeper9@naver.com'  // 전체 이메일 주소
```

## 체크리스트 (반드시 순서대로 확인!)

- [ ] **1단계**: 네이버 메일 외부 프로그램 사용 설정이 **"사용함"**인지 확인
- [ ] **2단계**: 네이버 메일 로그인 테스트 (아이디: beeper9, 비밀번호: kimjungbae99)
- [ ] **3단계**: 2단계 인증 활성화 여부 확인
- [ ] **4단계**: 2단계 인증 활성화 시 앱 비밀번호 생성 및 코드 수정
- [ ] **5단계**: Edge Function 재배포
- [ ] **6단계**: 평가 입력 후 이메일 발송 테스트
- [ ] **7단계**: Edge Function 로그 확인

## 문제 해결 순서

1. **가장 먼저**: 네이버 메일 외부 프로그램 사용 설정 확인 (가장 중요!)
2. **두 번째**: 네이버 계정 비밀번호 확인
3. **세 번째**: 2단계 인증 확인 및 앱 비밀번호 생성
4. **네 번째**: 코드 수정 및 Edge Function 재배포
5. **마지막**: 테스트 및 로그 확인

## 여전히 실패하는 경우

위의 모든 사항을 확인했는데도 여전히 실패한다면:

1. **네이버 고객센터 문의**
   - 네이버 메일 고객센터에 SMTP 접근 문제 문의
   - 계정 보안 정책 확인

2. **대안 이메일 서비스 고려**
   - Resend API
   - SendGrid API
   - Mailgun API

3. **Edge Function 로그 확인**
   - Supabase 대시보드 → Edge Functions → send-evaluation-email → Logs
   - "사용자명 인증 시도", "비밀번호 인증 시도" 메시지 확인
   - 실제 전송되는 값 확인

